// Placeholder auth configuration
// Can be implemented with your preferred authentication solution

export const authConfig = {
  // Authentication configuration will go here
  providers: [],
  debug: process.env.NODE_ENV === "development",
}
