"use client"

import type React from "react"

export function AuthProvider({ children }: { children: React.ReactNode }) {
  // Placeholder auth provider - can be implemented with your preferred auth solution
  return <>{children}</>
}
